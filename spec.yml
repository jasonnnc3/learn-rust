name: zero2prod
region: sfo
services:
  - name: zero2prod
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    dockerfile_path: Dockerfile
    source_dir: .
    github:
      branch: main
      deploy_on_push: true
      repo: jasonnnc3/learn-rust
    health_check:
      http_path: /health_check
    routes:
      - path: /
